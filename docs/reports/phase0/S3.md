---
report: AVG-S3 vision in Claude Desktop
phase: 0
status: accepted
authored_by: "Template + pre-run findings: Claude Code (Opus 5.5), 2026-09-23. Observed: Jim (JPEG export, Claude Desktop run and three answers, 2026-09-26); Claude Code (smoke check, Claude Code CLI run, Desktop log, 2026-09-26). Analysis, Numbers, Consequences: Claude Code (Opus 5.5), 2026-09-26. Verdict: Jim (Conditional go, 2026-09-26)."
date: 2026-09-26 (run)
---

# AVG-S3 — vision in Claude Desktop (and Claude Code)

## Purpose

Does an MCP tool result containing a 1600 px q75 JPEG `image` block get displayed in Claude Desktop, and can Claude describe it? The same question for Claude Code CLI.
PHASES.md rule, quoted: "Go: Desktop renders. No-go: re-decide client (AVG-002)."

## Harness

- `spikes\S3\server.ts` (tool `get_fixture_preview {name}`), `spikes\S3\smoke-client.ts`, `spikes\S3\probe-decoders.ts`.
- `spikes\S3\install-desktop-config.ts` adds the server to Claude Desktop's config; `spikes\S3\claude-code.mcp.json` is the config for the Claude Code CLI run (the older `claude_desktop_config.snippet.json` was not needed).
- `spikes\S3\summarize-cli-run.ts` prints the Claude Code CLI run's facts and writes the committed, trimmed copy of it.
- How to run: `spikes\S3\README.md`.

## Pre-run findings (Claude Code)

**1. sharp cannot decode the fixtures, so Jim needs to export one JPEG.** Handle: `node spikes\S3\probe-decoders.ts`, 2026-09-23 (libtiff `DNGPrivateData` / `error in tile` warnings filtered out):

```
sharp 0.35.4 / libvips 8.18.6 / node v24.11.1
20250413-_OZ81430.NEF
  default:            FAIL Input file contains unsupported image format
20260110-_Z8A0138-DxO_DeepPRIME XD3.dng
  default:            FAIL Warning treated as error due to failOn setting
  failOn=none subifd=0: FAIL vips_colourspace: no known route from 'multiband' to 'srgb'
  failOn=none subifd=1: decoded tiff 8256x5504 -> jpeg 1600x1067 10316 B, mean RGB 0.0,0.0,0.0
20260110-_Z8A0173.NEF
  default:            FAIL Input file contains unsupported image format
20260906-_OZ80005.NEF
  default:            FAIL Input file contains unsupported image format
20260907-_OZ80093.NEF
  default:            FAIL Input file contains unsupported image format
20260907-_OZ80099.NEF
  default:            FAIL Input file contains unsupported image format
```

With `failOn: "none"` and no `subifd`, the DNG decodes only its 258×172 IFD0 thumbnail (scratch probe, same session). The directive allows "use the DNG or ask Jim to export one JPEG". The DNG gives a 258 px thumbnail or a black frame, so a Lightroom JPEG export is needed (`spikes\S3\README.md`).

**2. Server plumbing works** (no Claude involved). Handle: `node spikes\S3\smoke-client.ts`, 2026-09-23:
- NEF → `isError: true`, text `sharp could not render 20260907-_OZ80093.NEF: Input file contains unsupported image format`.
- Synthetic 3000×2000 JPEG (via `LRC_AVG_FIXTURES_DIR`, a scratch folder) → `image block: mimeType=image/jpeg base64_chars=17216 decoded_bytes=12912 jpeg_soi=true` plus text `size=1600x1067 quality=75 render_ms=55`.

## Observed (Jim)

<!-- Nothing to paste. Jim tells Claude Code in chat: image shown in Claude Desktop yes/no, description right yes/no/partly [stated].
     Claude Code records those answers here, plus its own checks: smoke-client output, the Claude Code CLI run
     (spikes\S3\claude-code.mcp.json), and Claude Desktop's server log if needed. -->
Jim ran `spikes\S3\README.md` "Steps for Jim" on 2026-09-26 [stated].

- **Step 1, the export:** `fixtures\20260907-_OZ80093.jpg` (fixtures are gitignored), 2048×1365, sRGB with an ICC profile, 1,458,043 bytes [handle: `sharp(...).metadata()` → "2048x1365 jpeg space=srgb icc=true" and `(Get-Item …).Length`, run by Claude Code on 2026-09-26].
- **Steps 2–3, config and restart:** Claude Desktop's config file (Microsoft Store install, `%LOCALAPPDATA%\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude_desktop_config.json`) was rewritten at 10:21:10 local time, with a backup `…backup-2026-09-26T17-21-10-166Z` next to it [handle: file listing by Claude Code]. The server started at 17:21:38Z (UTC) [handle: `S3\desktop-mcp-log-excerpt.txt`].
- **Step 4, the prompt:** Claude Desktop called the tool at 17:22:21Z, and the server returned `result(2 blocks)` after rendering "1600x1066 332604 B in 102 ms" [handle: the same excerpt].

Jim's answers, in chat, 2026-09-26 [stated]:

| Question | Jim's answer |
|---|---|
| Was the image shown in Claude Desktop's answer? | "The image was NOT shown in the desktop." |
| With the `get_fixture_preview` tool-call box expanded, was the image there? | **Yes**: "Expanded: image there" (chosen from options Claude Code offered) |
| Was Claude Desktop's description right? | "Claude Desktop's description was correct." |
| Was Claude Code's description right? | "Claude Code's description was correct." |

Claude Code's own checks, 2026-09-26:

- **Smoke check on the real export:** `node spikes\S3\smoke-client.ts 20260907-_OZ80093.jpg` → one tool listed, `isError: false`, `image block: mimeType=image/jpeg base64_chars=443472 decoded_bytes=332604 jpeg_soi=true`, size 1600×1066, quality 75 [handle: `S3\smoke-client.out.txt`].
- **Claude Code CLI run** (started 10:23:52 local; `duration_ms` 34985):
  - Command: `claude -p "<the README's prompt>" --mcp-config spikes\S3\claude-code.mcp.json --strict-mcp-config --allowedTools "mcp__lrc-avg-spike-s3__get_fixture_preview" --output-format stream-json --verbose`, Claude Code 2.1.283, model `claude-opus-5-5[1m]`.
  - Summary: `node spikes\S3\summarize-cli-run.ts <run.jsonl> docs\reports\phase0\S3\s3_cli_run_redacted.jsonl` → `S3\summarize-cli-run.out.txt`. The committed copy of the run is `S3\s3_cli_run_redacted.jsonl`: the base64 image and thinking signatures are removed, and the start-up event is cut to the fields used here, because its full form lists every plugin and skill installed on Jim's machine and the repo is public. Local paths are scrubbed too (the home folder becomes `~`). The full run stayed in the session scratchpad.
  - The CLI also saved the tool's image to a local file, named in the tool result's `[Image: source: …]` text (the path is removed from the committed copy). That file is 1600×1066 and 332,604 bytes, the same size as the image the server sent [handle: `Get-Item` + `sharp(...).metadata()` on that file, run by Claude Code on 2026-09-26].

## Analysis (Claude Code, 2026-09-26)

- **Claude Desktop passes an MCP `image` block to the model, and Claude can read it.**
  - The server log shows the tool call and a 2-block result carrying the 1600×1066, 332,604-byte JPEG [handle: `S3\desktop-mcp-log-excerpt.txt`].
  - Jim judged Desktop's description of the photo correct [stated].
  - Since only the image could have told the model what the photo shows [inference: the tool's text block holds only the file name, byte count, size and quality], the model received and used the image.
- **Desktop shows the image to the user only inside the tool-call box.** It did not appear in the answer itself; it did appear when Jim expanded the `get_fixture_preview` box [stated].
- **The Claude Code CLI behaves the same way for the model.**
  - The MCP server connected; the tool result held an `image/jpeg` block of 443,472 base64 chars (332,604 bytes, valid JPEG start marker) plus the text block [handle: `S3\summarize-cli-run.out.txt`].
  - Claude's description (lava-boulder field below a stone tower with a chain railing, a ridge of dead trees, hard high sun from the upper left, cyan-blue sky, and a list of technical faults) was judged correct by Jim [stated; the full text is in `S3\summarize-cli-run.out.txt`].
  - `-p` mode prints text only, so "displayed" does not apply to this run. Whether the interactive CLI shows the image is [unverified].
- **No size problems at the NFR-3 payload.** 1600 px long edge, q75, one image per result, 332,604 bytes: no errors, truncation or size warnings in the Desktop log, and `is_error: false` in the CLI run. Larger or multiple images were not tried.
- **sharp decodes Lightroom's JPEG exports fine**, unlike the raw files (pre-run finding 1). It rendered the 2048 px export to 1600 px in 90–102 ms [handle: `S3\smoke-client.out.txt` ("render_ms=96" in the saved re-run), the Desktop log excerpt ("in 102 ms") and the CLI tool result ("render_ms=90")].

## Numbers

| Field | Claude Desktop | Claude Code CLI |
|---|---|---|
| Fixture JPEG used (name, exported size) | `20260907-_OZ80093.jpg`, exported 2048×1365, 1,458,043 B; served 1600×1066 q75 | same file, same rendering |
| Tool listed after restart? | yes (`tools/list` at 17:21:39Z, then called) | yes (`lrc-avg-spike-s3=connected`, tool listed) |
| Image displayed in the tool result? | **only inside the expanded tool-call box**, not in the answer itself [stated] | n/a in `-p` mode; the image reached the model (tool result `image/jpeg`) |
| Claude described it accurately? (Jim's judgement) | yes [stated] | yes [stated] |
| `jpeg_bytes` reported | 332,604 (log: "1600x1066 332604 B") | 332,604 (tool result text) |
| Errors / truncation / size warnings | none (`result(2 blocks)`) | none (`is_error: false`, decoded bytes = 332,604, JPEG start marker present) |
| Time to render the preview | 102 ms | 90 ms |

## Verdict

<!-- go | no-go (Jim) -->
**Conditional go: S3 is done** (Jim, 2026-09-26, chosen from the options Claude Code offered, which recommended Conditional go) [stated].

- **AVG-002 stands:** Claude Desktop stays the runtime client. The loop needs Claude's vision to get the previews, and it does [handle: `S3\desktop-mcp-log-excerpt.txt` tool call + Jim's "description was correct"].
- **The condition:** previews are not shown in the chat answer itself [stated: Jim, "The image was NOT shown"]. Jim sees a preview by expanding the tool-call box [stated: "Expanded: image there"], or looks at the photo in Lightroom.
- The option Jim chose was worded before his tool-box answer ("previews aren't shown to you in chat, so you judge edits and pick variants in Lightroom"). With that answer, the condition as recorded here is the narrower one above.

## Consequences / open questions

<!-- AVG-002 stands or is revisited; golden previews for tests/golden come from Lightroom exports, not sharp. -->
Proposed changes; none has been applied to the vault docs. They are for the architect (Jim).

- **AVG-002 (Claude Desktop as runtime client): confirmed, with the display condition above.**
- **PRD §6.6 step 5 and AVG-008, picking a variant** (`PRD.md:134`, "Jim picks in the HUD or in chat"). S3 tested one single JPEG, not the A/B/C contact sheet that `lr_begin_session` returns (`MCP_TOOLS.md:26`). A contact sheet is also one image per result under NFR-3 (`PRD.md:211`), so it should reach Claude and show in the expanded tool-call box the same way [inference]. Whether it does, and whether it is readable enough to pick from, is [unverified] until Phase 4. The copies are also real catalog photos Jim can compare in Lightroom (S6) [inference]. Proposal: the PRD says previews are not inline in the answer, so nobody expects the sheet there.
- **PRD NFR-3 (`PRD.md:211`)** — ≤ 1600 px, q75, one image per result — worked unchanged in both clients at 332,604 bytes [handle: Numbers]. Nothing to change.
- **Golden previews (`tests\golden\`) must come from Lightroom exports**, as the template noted: sharp cannot decode the NEFs (pre-run finding 1) but decodes Lightroom's JPEG export [handle: `S3\smoke-client.out.txt`].
- **The S3 server stays in Claude Desktop's config** until someone removes it: an `lrc-avg-spike-s3` entry, with a backup of the previous file next to it. It is harmless, but it starts a Node process with every Desktop launch [inference from the log's start-up lines]. Removing it is a Phase 1 set-up step, when the engine takes its place.
- **Still [unverified] after S3:**
  - whether the interactive Claude Code CLI shows the image (only `-p` was run);
  - how Desktop copes with many image results in one conversation (context growth over a 4-pass session plus variants);
  - whether a composite A/B/C sheet at 1600 px is readable enough for Jim to pick from;
  - which model Claude Desktop used (not in the MCP log);
  - repeatability: one run per client.
